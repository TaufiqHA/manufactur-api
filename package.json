{
  "name": "gondolaflow-mes-backend",
  "version": "1.0.0",
  "description": "Backend API for GondolaFlow MES",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "migrate": "node -e \"(async () => { const MigrationRunner = require('./migrations/migrationRunner'); const runner = new MigrationRunner(); await runner.runMigrations(); })();\"",
    "rollback": "node -e \"(async () => { const RollbackRunner = require('./migrations/rollbackRunner'); const runner = new RollbackRunner(); await runner.rollbackMigrations(1); })();\"",
    "rollback:all": "node -e \"(async () => { const RollbackRunner = require('./migrations/rollbackRunner'); const runner = new RollbackRunner(); await runner.rollbackAllMigrations(); })();\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0",
    "express-validator": "^7.3.1",
    "helmet": "^6.1.5",
    "joi": "^17.9.2",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.11.0",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "nodemon": "^2.0.22",
    "supertest": "^6.3.3"
  }
}
